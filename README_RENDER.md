# Render 部署

推荐用 Render 的 Static Site：

1. 把整个 `eason_blog` 文件夹上传到 GitHub 仓库。
2. Render → New → Static Site → 选择仓库。
3. Build Command 留空。
4. Publish Directory 填：`eason_blog`
5. 保存并部署。

如果使用本 ZIP 里的 `render.yaml`，Render Blueprint 会自动识别为静态站点。
